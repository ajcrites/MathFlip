# Local iOS devices

Copy this file to `docs/devices.md` and replace the placeholder values. The local file is gitignored because device identifiers are machine-specific.

| Role | Device | Identifier |
| --- | --- | --- |
| Primary | Device name | DEVICE-ID |
